import React from 'react';
import ReactDOM from 'react-dom/client';
import StockPortfolioTracker from './StockPortfolioTracker.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StockPortfolioTracker />
  </React.StrictMode>
);
